import React from "react";
import { createRoot } from "react-dom/client";
import AgencyFlow from "./App.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <AgencyFlow />
  </React.StrictMode>
);
